Maximum Sum Subarray in an array.
If previous sum is bad (negative), throw it away.

Advanced Exception Translation
Fluent Validation Framework

Design patterns are not about code — they are about structuring systems so that change is cheap and failures are isolated.

