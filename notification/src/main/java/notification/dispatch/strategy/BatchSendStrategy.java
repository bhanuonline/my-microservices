package notification.dispatch.strategy;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationContext;/** * Batch multiple contexts and send in one gateway call. * TODO: accumulate window (time-based or count-based), then flush. */@Slf4jpublic class BatchSendStrategy implements SendStrategy {    @Override    public void send(NotificationContext ctx) {        log.info("[stub] BatchSendStrategy — queue for batch, recipient={}", ctx.getRecipient());        // TODO: real batching + periodic flush    }}