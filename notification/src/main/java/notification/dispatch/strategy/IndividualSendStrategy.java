package notification.dispatch.strategy;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationContext;/** * One send call per recipient — simple, no batching. * TODO: wire to real EmailGateway / SmsGateway. */@Slf4jpublic class IndividualSendStrategy implements SendStrategy {    @Override    public void send(NotificationContext ctx) {        log.info("[stub] IndividualSendStrategy — send to {}", ctx.getRecipient());        // TODO: call gateway.send(ctx)    }}