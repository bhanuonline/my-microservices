package notification.dispatch;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationContext;import notification.core.NotificationType;import notification.dispatch.strategy.SendStrategy;/** * Email-specific dispatcher. Selects a SendStrategy (individual vs batch) * and hands off. * TODO: inject SendStrategy per context (feature flag driven). */@Slf4jpublic class EmailDispatcher implements OutboundDispatcher {    @Override    public void dispatch(NotificationContext ctx) {        if (ctx.getType() != NotificationType.EMAIL) return;        log.info("[stub] EmailDispatcher.dispatch for {}", ctx.getRecipient());        // TODO: SendStrategy strategy = resolver.resolve(ctx);        //       strategy.send(ctx);    }}