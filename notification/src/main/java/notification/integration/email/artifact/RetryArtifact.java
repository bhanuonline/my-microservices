package notification.integration.email.artifact;/** * Retry state persisted so a worker can resume from where a previous send failed. * TODO: store in DB; workers pick up PENDING rows with exponential backoff. */public record RetryArtifact(        String artifactId,        String originalPayloadId,        int attemptCount,        long nextAttemptAt,        String lastError) {}