package notification.integration.email.impl;import lombok.extern.slf4j.Slf4j;import notification.integration.email.model.EmailPayload;/** * Concrete SMTP gateway. * TODO: use jakarta.mail or spring-boot-starter-mail; configure host/port/auth * via yml; wrap in @Retry / @CircuitBreaker for gateway resilience. */@Slf4jpublic class SmtpEmailGateway {    public void send(EmailPayload payload) {        log.info("[stub] SmtpEmailGateway.send to={} subject={}",                payload.getTo(), payload.getSubject());        // TODO: real SMTP send    }}