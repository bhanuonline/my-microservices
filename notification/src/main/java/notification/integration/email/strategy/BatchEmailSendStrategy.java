package notification.integration.email.strategy;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationContext;import notification.dispatch.strategy.SendStrategy;import notification.integration.email.impl.SmtpEmailGateway;/** * Email-flavored BatchSendStrategy — accumulate then flush. * TODO: window-based flush (5s or 100 messages, whichever first). */@Slf4j@RequiredArgsConstructorpublic class BatchEmailSendStrategy implements SendStrategy {    private final SmtpEmailGateway gateway;    @Override    public void send(NotificationContext ctx) {        log.info("[stub] BatchEmailSendStrategy — queue {}", ctx.getRecipient());        // TODO: add to buffer; scheduled flush task calls gateway.send() for each    }}