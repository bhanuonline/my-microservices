package notification.integration.email.strategy;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationContext;import notification.dispatch.strategy.SendStrategy;import notification.integration.email.impl.SmtpEmailGateway;/** * Email-flavored IndividualSendStrategy — one SMTP call per recipient. * TODO: build EmailPayload from ctx and hand to gateway.send(). */@Slf4j@RequiredArgsConstructorpublic class IndividualEmailSendStrategy implements SendStrategy {    private final SmtpEmailGateway gateway;    @Override    public void send(NotificationContext ctx) {        log.info("[stub] IndividualEmailSendStrategy — {}", ctx.getRecipient());        // TODO: gateway.send(EmailPayload.builder()...build());    }}