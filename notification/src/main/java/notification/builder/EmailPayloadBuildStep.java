package notification.builder;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationContext;import notification.core.NotificationType;import org.springframework.stereotype.Component;/** * Assembles the final email payload (subject, body, headers) from context + template. * TODO: render template with variables; add MIME parts / attachments. */@Slf4j@Componentpublic class EmailPayloadBuildStep implements BuildStep {    @Override    public void execute(NotificationContext ctx) {        if (ctx.getType() != NotificationType.EMAIL) return;        log.debug("[stub] EmailPayloadBuildStep — would render template for {}", ctx.getRecipient());        // TODO: populate ctx.metadata with rendered subject + body    }}