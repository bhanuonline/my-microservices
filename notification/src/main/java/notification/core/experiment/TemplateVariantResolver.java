package notification.core.experiment;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import notification.core.feature.FeatureFlagService;import notification.core.template.TemplateContext;import notification.core.template.VariantResolver;/** * A/B variant picker driven by feature flags. * TODO: real experiment framework (multi-arm bandit / hash-based bucketing). */@Slf4j@RequiredArgsConstructorpublic class TemplateVariantResolver implements VariantResolver {    private final FeatureFlagService flags;    @Override    public String resolveVariant(String templateId, TemplateContext ctx) {        log.warn("[stub] TemplateVariantResolver.resolveVariant({})", templateId);        return templateId;   // default: no variant    }}