package notification.core.feature;import lombok.extern.slf4j.Slf4j;/** * FeatureFlagService that fetches flag state from a remote provider. * TODO: implement — HTTP client with cache + refresh. */@Slf4jpublic class RemoteFeatureFlagService implements FeatureFlagService {    @Override    public boolean isEnabled(String flagKey) {        log.warn("[stub] RemoteFeatureFlagService.isEnabled({})", flagKey);        return false;    }    @Override    public boolean isEnabled(String flagKey, String contextKey) {        log.warn("[stub] RemoteFeatureFlagService.isEnabled({}, {})", flagKey, contextKey);        return false;    }}