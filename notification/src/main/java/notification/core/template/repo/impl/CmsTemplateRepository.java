package notification.core.template.repo.impl;import lombok.extern.slf4j.Slf4j;import notification.core.template.Template;import notification.core.template.TemplateRepository;/** * Repository backed by a CMS. * TODO: implement — delegate to CmsTemplateProvider, add retry logic. */@Slf4jpublic class CmsTemplateRepository implements TemplateRepository {    @Override    public Template findById(String templateId) {        log.warn("[stub] CmsTemplateRepository.findById({})", templateId);        return new Template(templateId, "");    }}