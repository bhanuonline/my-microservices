package notification.core.template;import lombok.extern.slf4j.Slf4j;import notification.core.NotificationType;import java.util.Optional;/** * Loads templates from local filesystem. * TODO: implement — read from classpath:templates/{type}/{lang}/{version}/{variant}.html. */@Slf4jpublic class FileBasedTemplateProvider implements TemplateProvider {    @Override    public Optional<Template> find(NotificationType type, String language, String version, String variant) {        log.warn("[stub] FileBasedTemplateProvider.find(type={}, lang={}, ver={}, var={})",                type, language, version, variant);        return Optional.empty();    }}