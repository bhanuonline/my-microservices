package notification.context;import lombok.extern.slf4j.Slf4j;import org.springframework.boot.context.event.ApplicationReadyEvent;import org.springframework.boot.web.context.WebServerApplicationContext;import org.springframework.context.event.EventListener;import org.springframework.stereotype.Component;import org.springframework.web.method.HandlerMethod;import org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping;import java.util.Map;/** * On startup, logs every HTTP endpoint the service exposes. * Useful debugging aid — prints once, gone forever. */@Slf4j@Componentpublic class EndpointPrinter {    @EventListener(ApplicationReadyEvent.class)    public void printEndpoints(ApplicationReadyEvent event) {        var ctx = event.getApplicationContext();        if (!(ctx instanceof WebServerApplicationContext)) return;   // notification has no web server → skip        try {            RequestMappingHandlerMapping mapping = ctx.getBean(RequestMappingHandlerMapping.class);            Map<?, HandlerMethod> methods = mapping.getHandlerMethods();            log.info("======== HTTP endpoints ({}) ========", methods.size());            methods.forEach((info, method) -> log.info("  {}  →  {}", info, method));            log.info("=====================================");        } catch (Exception e) {            log.debug("EndpointPrinter skipped: {}", e.getMessage());        }    }}