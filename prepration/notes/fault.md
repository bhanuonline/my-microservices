Mistake 1: Optional should not be used as a field ❌ I Used when I was learning @Java8FTProgrammingTest.java
Optional is intended for return types, not class fields.

Mistake 2
Collectors.groupingBy() is not used with map(). It is used with collect().