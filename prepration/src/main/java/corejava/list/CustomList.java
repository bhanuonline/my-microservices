package corejava.list;import java.util.ArrayList;import java.util.Objects;class MyCustomList<T> {    private Object[] data;    private int size = 0;    public MyCustomList() {        this.data = new Object[10];    }    public void add(T item) {        grow();        data[size] = item;        size++;    }    private void grow() {        if (size == data.length) {            int newCapacity = data.length * 2;            Object[] newData = new Object[newCapacity];            for (int i = 0; i < size; i++) {                newData[i] = data[i];            }            data = newData;        }    }    @Override    public String toString() {        StringBuilder result = new StringBuilder("[");        for (int i = 0; i < size; i++) {            result.append(data[i]);            if (i < size - 1) {                result.append(", ");            }        }        result.append("]");        return result.toString();    }    public void add(int index, T item) {        checkIndex(index);        grow();        for (int i = size; i > index; i--) {            data[i] = data[i - 1];        }        data[index] = item;        size++;    }    private void checkIndex(int index) {        if (index < 0 || index > data.length) {            throw new IndexOutOfBoundsException("Index is not valid " + index);        }    }    public int size() {        return size;    }    public boolean contains(T item) {        for (int i = 0; i < size; i++) {            if (Objects.equals(data[i], item)) {                return true;            }        }        return false;    }    public T set(int index,T item) {        checkIndex(index);        T old = (T) data[index];        data[index] = item;        return old;    }    public T remove(int index){        return null;    }    public boolean remove(Object o){        return false;    }}public class CustomList {    public static void main(String[] args) {        ArrayList<Integer> arr = new ArrayList<>();        arr.add(10);        System.out.println(arr.set(0, 20));        System.out.println(arr);        MyCustomList<Integer> mylist = new MyCustomList<>();        mylist.add(12);        mylist.add(13);        mylist.add(14);        mylist.add(2, 19); //12 13 19 14        mylist.add(20);        mylist.add(21);        mylist.add(15);        mylist.add(18);        mylist.add(10);        mylist.add(10);        mylist.add(14);        mylist.add(14);        System.out.println(mylist);        System.out.println("\n" + mylist.size());        System.out.println(mylist.contains(12));        mylist.set(0,100);        System.out.println("list after new element :"+mylist);    }}