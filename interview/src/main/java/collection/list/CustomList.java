package collection.list;import java.util.ArrayList;import java.util.List;import java.util.Objects;/** * Custom implementation of a simple List. * * Internally, the list uses an Object[] array. * * Main concepts: * 1. Add element * 2. Grow array when capacity is full * 3. Get element by index * 4. Set/replace element * 5. Add element at a specific index * 6. Remove element by index * 7. Remove element by value * 8. Search using indexOf / lastIndexOf * 9. Clear all elements */class MyList<T> {    private int size;    private Object[] data;    /**     * Default capacity of the list.     */    public MyList() {        this.data = new Object[10];        this.size = 0;    }    /**     * Adds an element at the end of the list.     *     * Example:     * [10, 20, 30]     *     * add(40)     *     * [10, 20, 30, 40]     */    public void add(T item) {        grow();        data[size] = item;        size++;    }    /**     * Increases the capacity when the array is full.     *     * Current capacity = 10     * New capacity     = 20     *     * Existing elements are copied into the new array.     */    private void grow() {        if (size == data.length) {            int newCapacity = data.length * 2;            Object[] newArr = new Object[newCapacity];            // Copy existing elements            for (int i = 0; i < size; i++) {                newArr[i] = data[i];            }            data = newArr;        }    }    /**     * Returns the number of elements currently stored.     *     * Important:     * size != data.length     *     * size     -> number of actual elements     * capacity -> total available array positions     */    public int size() {        return size;    }    /**     * Returns the element at the specified index.     */    @SuppressWarnings("unchecked")    public T get(int index) {        check(index);        return (T) data[index];    }    /**     * Checks whether the index is valid.     *     * Valid index:     * 0 to size - 1     *     * Example:     * size = 5     *     * Valid indexes = 0, 1, 2, 3, 4     */    private void check(int index) {        if (index < 0 || index >= size) {            throw new IndexOutOfBoundsException(                    "Index is not valid: " + index            );        }    }    /*     * ADD AT INDEX     *     * Example:     *     * Before:     * [10, 20, 30, 40]     *     * add(1, 100)     *     * We need to make space at index 1.     *     * SHIFT RIGHT     *     * Start from RIGHT side to avoid overwriting data.     *     * [10, 20, 30, 40]     *          ↓     * [10, 20, 30, 30, 40]     *     * Result:     * [10, 100, 20, 30, 40]     */    public void add(int index, T item) {        // For add(), index == size is also valid.        if (index < 0 || index > size) {            throw new IndexOutOfBoundsException(                    "Index is not valid: " + index            );        }        grow();        // SHIFT RIGHT        for (int i = size; i > index; i--) {            data[i] = data[i - 1];        }        data[index] = item;        size++;    }    /**     * Replaces the element at the specified index.     *     * Returns the old value.     *     * Example:     *     * Before:     * [10, 20, 30]     *     * set(1, 100)     *     * After:     * [10, 100, 30]     *     * Returns:     * 20     */    public T set(int index, T item) {        check(index);        T old = get(index);        data[index] = item;        return old;    }    /**     * Checks whether the given element exists in the list.     *     * Objects.equals() safely handles null values.     */    public boolean contains(T item) {        for (int i = 0; i < size; i++) {            if (Objects.equals(data[i], item)) {                return true;            }        }        return false;    }    /**     * Removes an element using its index.     *     * Example:     *     * Before:     * [10, 20, 30, 40]     *     * remove(1)     *     * Remove 20.     *     * SHIFT LEFT     *     * [10, 30, 40, 40]     *     * Then last duplicate position is cleared:     *     * [10, 30, 40, null]     *     * Final:     * [10, 30, 40]     */    public T remove(int index) {        check(index);        T old = get(index);        // SHIFT LEFT        for (int i = index; i < size - 1; i++) {            data[i] = data[i + 1];        }        // Remove duplicate reference at the end        data[size - 1] = null;        size--;        return old;    }    /**     * Removes the first occurrence of the given object.     *     * Example:     *     * [10, 20, 30, 20]     *     * remove(Integer.valueOf(20))     *     * Result:     * [10, 30, 20]     *     * Only the FIRST 20 is removed.     */    public boolean remove(Object item) {        for (int i = 0; i < size; i++) {            if (Objects.equals(data[i], item)) {                // SHIFT LEFT                for (int j = i; j < size - 1; j++) {                    data[j] = data[j + 1];                }                // Remove duplicate reference                data[size - 1] = null;                size--;                return true;            }        }        // Element was not found        return false;    }    /**     * Returns the index of the first occurrence.     *     * Example:     *     * [10, 20, 30, 20]     *     * indexOf(20) -> 1     */    public int indexOf(T item) {        for (int i = 0; i < size; i++) {            if (Objects.equals(data[i], item)) {                return i;            }        }        return -1;    }    /**     * Returns the index of the last occurrence.     *     * Example:     *     * [10, 20, 30, 20]     *     * lastIndexOf(20) -> 3     *     * Start searching from RIGHT to LEFT.     */    public int lastIndexOf(T item) {        for (int i = size - 1; i >= 0; i--) {            if (Objects.equals(data[i], item)) {                return i;            }        }        return -1;    }    /**     * Removes all elements from the list.     *     * We set each reference to null so that     * objects can be garbage collected.     *     * After clear():     *     * size = 0     *     * Capacity remains unchanged.     */    public void clear() {        for (int i = 0; i < size; i++) {            data[i] = null;        }        size = 0;    }    /**     * Returns true if the list contains no elements.     *     * Empty list:     * size = 0     */    public boolean empty() {        return size == 0;    }    /**     * Converts the list into String format.     */    @Override    public String toString() {        StringBuilder result = new StringBuilder("[");        for (int i = 0; i < size; i++) {            result.append(data[i]);            if (i < size - 1) {                result.append(", ");            }        }        result.append("]");        return result.toString();    }}/** * Test class for MyList. */public class CustomList {    public static void main(String[] args) {        /*         * =========================================================         * JAVA ARRAYLIST EXAMPLE         * =========================================================         */        List<Integer> list = new ArrayList<>();        list.add(101);        list.add(20);        list.add(20);        list.add(10);        list.add(20);        list.add(20);        // Removes first occurrence of value 20        // list.remove(Integer.valueOf(20));        // Removes all elements        list.clear();        System.out.println("ArrayList indexOf(20): "                + list.indexOf(20));        System.out.println("ArrayList lastIndexOf(20): "                + list.lastIndexOf(20));        /*         * =========================================================         * CUSTOM LIST EXAMPLE         * =========================================================         */        MyList<Integer> myList = new MyList<>();        /*         * Add elements at the end.         */        myList.add(10);        myList.add(20);        myList.add(30);        myList.add(40);        myList.add(50);        myList.add(60);        myList.add(70);        myList.add(80);        myList.add(90);        myList.add(100);        myList.add(110);        /*         * Adding more than 10 elements triggers grow().         *         * Initial capacity = 10         * After 10 elements:         *         * size = 10         * capacity = 10         *         * Next add() calls grow().         *         * New capacity = 20         */        myList.add(10);        myList.add(20);        myList.add(30);        myList.add(40);        myList.add(50);        myList.add(60);        myList.add(70);        myList.add(80);        myList.add(90);        myList.add(100);        myList.add(110);        System.out.println("MyList: " + myList);        System.out.println("Size: " + myList.size());        /*         * GET         */        System.out.println(                "Element at index 0: " + myList.get(0)        );        /*         * =========================================================         * ADD AT INDEX         * =========================================================         *         * Before:         * [10, 20, 30, ...]         *         * add(0, 101)         *         * Existing elements shift RIGHT.         *         * After:         * [101, 10, 20, 30, ...]         */        myList.add(0, 101);        System.out.println("After add(0, 101): " + myList);        System.out.println("Size: " + myList.size());        /*         * =========================================================         * SET         * =========================================================         *         * Replace value at index 0.         */        Integer oldValue = myList.set(0, 102);        System.out.println("Old value: " + oldValue);        System.out.println("After set(): " + myList);        /*         * =========================================================         * CONTAINS         * =========================================================         */        System.out.println(                "Contains 10: " + myList.contains(10)        );        /*         * =========================================================         * REMOVE BY INDEX         * =========================================================         *         * remove(9)         *         * Removes the element at index 9.         *         * Elements after index 9 shift LEFT.         */        Integer removedValue = myList.remove(9);        System.out.println("Removed value: " + removedValue);        System.out.println(                "After remove(index): " + myList        );        /*         * =========================================================         * REMOVE BY VALUE         * =========================================================         *         * Remove first occurrence of 90.         */        boolean removed = myList.remove(Integer.valueOf(90));        System.out.println("90 removed: " + removed);        System.out.println(                "After remove(value): " + myList        );        /*         * =========================================================         * INDEX OF         * =========================================================         */        System.out.println(                "First index of 90: " + myList.indexOf(90)        );        /*         * =========================================================         * LAST INDEX OF         * =========================================================         */        System.out.println(                "Last index of 90: " + myList.lastIndexOf(90)        );        /*         * =========================================================         * ADD AT INDEX         * =========================================================         */        myList.add(0, 100);        System.out.println(                "After adding 100 at index 0: " + myList        );        /*         * =========================================================         * CLEAR         * =========================================================         */        myList.clear();        System.out.println(                "After clear(): " + myList        );        /*         * =========================================================         * EMPTY         * =========================================================         */        System.out.println(                "Is list empty: " + myList.empty()        );    }}