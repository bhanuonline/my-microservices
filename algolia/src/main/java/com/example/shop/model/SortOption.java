package com.example.shop.model;

public enum SortOption {
    RELEVANCE,       // default — searches main index
    PRICE_ASC,       // low to high
    PRICE_DESC,      // high to low
    RATING_DESC      // best rated first
}