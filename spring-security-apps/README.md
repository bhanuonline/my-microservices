setup :

For Theamleaf required mavan if will get the respource exception and page will not  local on browser

mvn clean install
mvn wrapper:wrapper [run to genrate teh wrapper]
./mvnw spring-boot:run [run the application]
